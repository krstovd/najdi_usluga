(() => { const status=document.querySelector('#provider-dashboard-status');const stats=document.querySelector('#provider-stats');function show(m,e=false){status.hidden=!m;status.textContent=m||'';status.classList.toggle('error',e);}function card(value,label){const c=document.createElement('article');c.className='stat-card';const strong=document.createElement('strong');strong.textContent=value;const span=document.createElement('span');span.textContent=label;c.append(strong,span);return c;} Promise.all([fetch('/api/providers/me'),fetch('/api/reservations/provider?size=100')]).then(async([p,r])=>{if(!p.ok)throw new Error(p.status===404?'Create your provider profile to begin.':'Provider access is required.');const provider=await p.json();const reservationBody=r.ok?await r.json():{content:[]};const all=reservationBody.content;stats.append(card(provider.verificationStatus,'Verification'),card(all.length,'Reservations'),card(all.filter(x=>x.status==='PENDING').length,'Pending'),card(all.filter(x=>x.status==='COMPLETED').length,'Completed'),card(provider.averageRating,'Average rating'),card(provider.reviewCount,'Reviews'));stats.hidden=false;show(provider.verificationStatus==='VERIFIED'?'':'Your profile is not public until an administrator verifies it.');}).catch(e=>show(e.message,true));})();
